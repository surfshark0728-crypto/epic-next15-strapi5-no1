import { registerUserService, loginUserService, getUserMeService } from "./auth";
export const services ={
    auth:{
        registerUserService,
        loginUserService,
        getUserMeService
    }
}

